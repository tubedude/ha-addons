#!/usr/bin/execlineb -S1
# If tailscaled dies, do NOT take the add-on down: local mosquitto keeps
# serving the LAN. Losing remote access beats losing the broker entirely.
foreground { redirfd -w 1 /dev/null echo "tailscaled saiu (${1})" }
exit 0
